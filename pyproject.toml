[tool.poetry]
name = "moj-projekt"
version = "0.1.0"
description = "Moje wprowadzenie do Pythona"
authors = ["Twój Nick <you@example.com>"]
readme = "README.md"
packages = [{include = "src"}]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0" 
